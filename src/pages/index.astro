---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("posts", ({ data }) => !data.draft))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<BaseLayout title="Aulia Rahman" description="Software engineer based in Jakarta. Frontend, full-stack, and figuring it out.">
  <h1 class="font-serif text-3xl mb-8 text-neutral-900">Aulia Rahman</h1>

  <p class="text-[15px] mb-4 leading-7">
    Software engineer in Jakarta. I've been building frontend with React and
    Next.js for a while, and recently started picking up Go to move closer to
    full-stack.
  </p>

  <p class="text-[15px] mb-4 leading-7">
    I work with Claude Code and Cursor every day. AI tooling changed how I
    write code — I ship faster and spend less time on the parts that don't
    require thinking.
  </p>

  <p class="text-[15px] mb-12 leading-7">
    Outside of work I play basketball, lift, and run. Find me
    on <a href="https://x.com/rhmnaul" target="_blank" rel="noopener">X</a>, <a href="https://github.com/rhmnaulia" target="_blank" rel="noopener">GitHub</a>, and <a href="https://linkedin.com/in/rhmnaulia/" target="_blank" rel="noopener">LinkedIn</a>.
  </p>

  <ul class="space-y-3">
    {posts.map((post) => (
      <li>
        <a href={`/blog/${post.id}`} class="font-serif text-[17px] text-neutral-900 decoration-neutral-300 hover:decoration-neutral-800">
          {post.data.title}
        </a>
      </li>
    ))}
  </ul>
</BaseLayout>
